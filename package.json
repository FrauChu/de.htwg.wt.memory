{
  "name": "memory",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "bower cache clean && bower install"
  },
  "license": "GPL",
  "dependencies": {
    "express": "^3.0.0",
    "bower": "^1.3.2"
  },
  "engines": {
    "node": "0.10.x"
  }
}